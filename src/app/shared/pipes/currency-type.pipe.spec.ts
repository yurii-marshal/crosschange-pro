import { CurrencyTypePipe } from './currency-type.pipe';

describe('CurrencyTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyTypePipe();
    expect(pipe).toBeTruthy();
  });
});
