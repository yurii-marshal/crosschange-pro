import { TimeBeforePipe } from './time-before.pipe';

describe('TimeBeforePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeBeforePipe();
    expect(pipe).toBeTruthy();
  });
});
